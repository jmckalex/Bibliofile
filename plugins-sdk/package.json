{
  "name": "@bibdesk/plugins-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure JS plugin API over an in-memory BibDesk library: query/mutate entries, macros, import/merge, duplicate detection, change events, plus a plugin manifest + manager. Foundation for third-party scripts and the Claude scripting assistant.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bibdesk/bibtex": "workspace:*",
    "@bibdesk/model": "workspace:*",
    "@bibdesk/formats": "workspace:*",
    "@bibdesk/tex": "workspace:*",
    "@bibdesk/names": "workspace:*"
  },
  "devDependencies": {}
}
