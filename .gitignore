node_modules/
dist/
out/
*.log
.DS_Store
coverage/
.vitest/
*.tsbuildinfo
.eslintcache

# Per-session handover doc: rewritten every session and edited on every feature
# branch, so tracking it guarantees cross-branch merge conflicts. Kept on disk
# (untracked) as the canonical resume doc; never commit it.
HANDOVER.md

# Native addon build output (ABI-specific; rebuilt via node-gyp)
app/native/**/build/
*.node

# electron-builder output
release/
