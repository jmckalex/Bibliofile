{
  "name": "@bibdesk/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Ported BibDesk type/field config (TypeInfo.plist + Preferences field-type arrays) as JSON + typed accessors.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {}
}
