// @bibdesk-stub — replace with real implementation.
/**
 * @bibdesk/config
 * Ported BibDesk type/field config (TypeInfo.plist + Preferences field-type arrays) as JSON + typed accessors.
 */
export const __package = 'config';
