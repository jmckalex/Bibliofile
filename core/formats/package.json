{
  "name": "@bibdesk/formats",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BDSKFormatParser cite-key + autofile mini-language, CRC32, sanitizers. Platform-agnostic.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bibdesk/model": "workspace:*",
    "@bibdesk/names": "workspace:*"
  },
  "devDependencies": {}
}
